build:
  docker:
    web: backend/Dockerfile
release:
  image: web
  command:
    - bash -c "python manage.py makemigrations && python manage.py migrate && gunicorn --bind 0.0.0.0:$PORT config.wsgi --log-file -"